

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(255);

  translate(width/2, height/2);
  fill(255);
  ellipse(0, 0, 400, 400);

  fill(0);
  ellipse(0, 0, 3, 3);

  for (let i=1; i >=60; i++) {
    
  }

}
